# 第二人工主集收口授权

> 授权编号：`AUTH-00-SECOND-PRIMARY-RESOLUTION-20260714`  
> 授权日期：2026-07-14  
> 用户原始指令：不管怎么样，把第二主集给我搞定了  
> 所属任务：10-M1–M2 数据与协议  
> 状态：`ACTIVE_UNTIL_SECOND_PRIMARY_FROZEN_OR_EVIDENCE_EXHAUSTED`

## 1. 目标

冻结一个合法、可复现、任务匹配的第二人工跨域图像主集，完成来源、许可、固定版本、文件树、逐图多人类真值、canonical映射、分组split、泄漏门和G1/G2证据。优先抢救LAI-GAI；若其核心资产不能闭合，立即切换经同一硬门审计的替代候选。

## 2. 授权动作

- 访问无需登录的官方项目页、论文页、数据仓库与公开API；
- 串行获取公开元数据、数据字典、README、manifest、评分表和为验证图像—评分映射所必需的公开数据资产；
- 下载公开、免费且许可明确的数据包，原始资产仅写入Git忽略的`data/raw/`；
- 计算文件大小、SHA-256，建立不可变manifest和审计报告；
- 构建不含个人标识的canonical人工分布、标签映射、分组split与泄漏测试；
- 在LAI-GAI无法满足硬门时切换OASIS或其他满足构念与权利要求的公开人工图像候选。

## 3. 资源与访问边界

- 仅使用公开、免费、无需登录的入口；禁止付费服务、绕过gating、代理规避、Cookie/token、秘密链接和第三方盗版镜像；
- OSF/API请求串行执行，目标间隔至少1.1秒；失败不通过降低门槛伪装为成功；
- 不联系作者、不代表用户签署EULA或机构协议；如最终只剩作者/机构动作才能完成，则形成精确请求包回报用户；
- 不提交原始图像、原始逐人响应、个人信息或大型数据到Git；只跟踪脱敏manifest、聚合统计、脚本和文档；
- 不训练模型、不创建任务20，直到G1/G2书面通过。

## 4. 冻结硬门

第二集只有同时满足下列条件才可冻结：

1. 官方来源、许可定位、固定revision或不可变下载快照明确；
2. 图像文件树非空，最终保留样本的图像与人工评分100%可追溯；
3. 真值来自独立人类诱发反应；生成prompt、目标类别或模型标签不充当真值；
4. 能按图像构造经验反应分布或预注册的连续分布目标，并保留样本量/不确定性；
5. T0内容输入、敏感字段、可发布边界和跨域角色明确；
6. 同图、精确/近重复、生成族/来源组和任何可测捷径不跨正式split；
7. manifest、canonical、split、label provenance和validator可重跑，无Critical失败。

## 5. 止损顺序

1. LAI-GAI：只补核心图像资产定位、公开资产与图像—评分映射，不重复旧26请求；
2. OASIS：若LAI-GAI失败，核900张开放图像、VA人工规范及可下载资产；
3. 其他候选：仅在前两者失败后按相同硬门检索；
4. 若所有公开候选均不满足，则不得伪造第二主集，必须回报唯一剩余的外部动作或正式修改投稿目标。

