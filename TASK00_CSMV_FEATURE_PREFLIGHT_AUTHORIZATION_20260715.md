# 任务00：CSMV正式输入资产最小预审授权

> 授权编号：`AUTH-00-CSMV-FEATURE-ASSET-PREFLIGHT-RO-20260715`  
> 授权日期：2026-07-15  
> 依据：`REVIEW-00-CSMV-LINEAGE-G2-20260715`

## 1. 授权目的

只读核验CSMV官方README链接的视觉特征资产，取得决定是否授权下载所需的最小准入元数据。该授权不等于资产可用、G2通过或任务20启动。

## 2. 允许范围

- 仅访问官方固定commit README明确链接的公开Google Drive特征目录及其同源公开说明页；无需登录、免费、公开可见。
- 可按`POLICY-00-LOCAL-PROXY-TRANSPORT-20260715`使用用户控制的本机代理作为传输通道；代理配置仅从本机环境变量或Git忽略配置读取，不得落盘到tracked文件或日志。
- 只读取页面/文件列表元数据：特征族名称、许可定位、访问条件、文件树/数量、总大小、单文件大小、revision/更新时间、公开checksum、8210内部`video_file_id`覆盖说明。
- 可保存脱敏页面元数据与响应hash到Git忽略目录；tracked报告不得包含Cookie、账户、秘密下载链接、原始URL清单或个人信息。
- 允许本地修改复现器与验证器：新增不依赖CUC银标根的公开benchmark核心隔离重跑，现场重算当前输出hash并fail-closed。

## 3. 禁止范围

- 不登录Google账户，不使用Cookie/token，不调用Google Drive API或第三方镜像，不绕过配额/权限；本机代理只作传输，不改变host、访问级别和metadata-only授权范围。
- 不下载任何`.npy`特征、视频、音频或完整数据包；不访问TikTok URL。
- 不联系作者、不接受EULA、不购买服务、不创建任务20、不训练、不建索引。
- 页面未显示的许可、revision、hash或文件树一律记`UNKNOWN`，不得推断为允许。

## 4. 回交要求

任务10须回交：官方入口与固定快照、资产级许可原文定位、gating、特征族矩阵、文件数量/体量/checksum覆盖、8210键覆盖可验证性、建议下载的唯一最小特征族及预计空间。只有00后续书面下载授权才能取得特征内容。
