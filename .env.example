# 只登记变量名，不填写真实凭证。复制为.env后由本机安全注入。
STEPFUN_API_KEY=
OPENAI_API_KEY=
HF_TOKEN=
WANDB_API_KEY=

# 可选：只在本机.env中填写代理地址；不得提交真实URL、用户名或密码。
HTTP_PROXY=
HTTPS_PROXY=
ALL_PROXY=
NO_PROXY=

# 可选：CSMV I3D只读特征根；留空时使用项目Git忽略目录中的默认junction。
CSMV_I3D_ROOT=
